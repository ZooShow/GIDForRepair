{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <div class="materialsArray" data-materials-array="{{ materials|json_encode|e('html_attr') }}"></div>
    <div class="toolsArray" data-tools-array="{{ tools|json_encode|e('html_attr') }}"></div>
    <div class="blocks" data-blocks="{{ article|json_encode|e('html_attr') }}"></div>
    <div id="app" class="container mt-5 shadow-5-strong ">
        <div class="card-header">Поделись с другими пользователями своими знаниями и умениями!</div>
        <div class="card-body over-hidden">
            <div id="editorJS" class="p-3" style="background: white"></div>
        </div>

        <div class="card-footer text-right">
            <p>Перед сохранением выбери тип ремонта и вид работ</p>
            <div class="form-outline">
                <select class="form-select select-input placeholder-active active" name="repairKind" id="repairKind">
                    <option value="Косметический ремонт" selected>Косметический ремонт</option>
                    <option value="Капитальный ремонт">Капитальный ремонт</option>
                    <option value="Евроремонт">Евроремонт</option>
                </select>
                <select class="form-select select-input placeholder-active active" name="repairType" id="repairType">
                </select>
            </div>
        </div>

        <div class="card-footer text-right">
            <select id="materials" class="form-select" data-mdb-placeholder="Выберите материалы" multiple>
            </select>
            <button id="btnAddMaterials" class="btn btn-light btn-rounded">
                Добавить материалы
            </button>
            <p id="materialsP" class="hidden">Материалы:</p>
        </div>

        <div class="card-footer text-right">
            <select id="tools" class="form-select" size="3" data-mdb-placeholder="Выберите материалы" multiple>
            </select>
            <button id="btnAddTools" class="btn btn-light btn-rounded">
                Добавить инструменты
            </button>
            <p id="toolsP" class="hidden">Инструменты:</p>
        </div>

        <div class="card-footer text-right">
            <p class="m-1">Нашли ошибку на сайте?</p>
            <button type="button" class="btn btn-light btn-rounded" data-mdb-toggle="modal"
                    data-mdb-target="#staticBackdrop">
                Свяжитесь с нами
            </button>
        </div>

        <div class="card-footer text-right">
            <button id="btnEdit" class="btn btn-light btn-rounded">
                Сохранить
            </button>
            <button id="btnReset" class="btn btn-light btn-rounded">
                Очистить
            </button>
        </div>
    </div>
{% endblock %}
